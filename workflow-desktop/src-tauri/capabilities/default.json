{
  "$schema": "https://schema.tauri.app/capabilities/2",
  "identifier": "default",
  "description": "Main window: core webview permissions plus the opener plugin for external links.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
