建议保留 `WORKFLOW_API_TOKEN` 和服务端鉴权，增加不依赖 `dsh-client` 的可见 PowerShell 登录引导。

实施步骤：

1. 增加用户级凭据存储/登录脚本，放在 `C:\Users\Administrator\.agents\workflow\settings`：
   - 新增可见的 `Login-WorkflowCredential.ps1`，通过 `Read-Host -AsSecureString` 获取邮箱和密码，调用现有的 `/api/v1/auth/client-login`。
   - 成功后只把返回的 bearer token 以当前用户 DPAPI 加密写入现有的 `secrets\workflow-api-token.dpapi`；不把密码、token 放进命令行、配置、日志或项目目录。
   - 保留现有 `Install-WorkflowCredential.ps1` 作为手工安装专用 machine/API token 的入口，不改变它的 stdin 约定。

2. 修改 `Start-WorkflowProcess.ps1`：
   - 启动命令前读取本机 DPAPI 凭据，并用 `/api/v1/auth/session` 验证 bearer 是否仍有效；验证成功时沿用现有环境变量注入逻辑。
   - 凭据不存在、已过期或被撤销时，使用 `Start-Process powershell.exe -WindowStyle Normal -Wait` 弹出一个真正可见的 PowerShell 登录窗口，运行 `Login-WorkflowCredential.ps1`；登录成功后重新读取凭据并继续原命令。
   - 网络不可达、TLS/证书失败或登录取消时直接返回明确错误，不绕过鉴权，也不把 token 打到输出。
   - 保持现有的 `WORKFLOW_STORAGE_MODE=database`、`WORKFLOW_DATABASE_PROVIDER=remote`、固定 API URL 和 finally 清理逻辑。
   - 为脚本增加一个防止父子脚本递归触发登录的内部开关，并保留非交互/手工 machine token 使用路径的可诊断错误。

3. 把凭据读写和 DPAPI 逻辑抽成用户级 settings 下的共享 helper，供安装脚本、登录脚本和启动脚本复用，避免三处各自处理秘密材料；权限继续限制为当前用户和 SYSTEM。

4. 更新用户级启动脚本的说明/错误文案，明确：没有安装 `dsh-client` 也可以通过 Workflow PowerShell 登录；`WORKFLOW_API_TOKEN` 仍是进程内变量，磁盘上只保存 DPAPI 密文。不要修改公开 Workflow API 的 `requireAction`、project scope 或匿名访问行为。

5. 验证：
   - 用 PowerShell parser 做全部脚本语法检查。
   - 用本地临时 HTTPS/HTTP 测试服务覆盖 token 缺失时弹出登录、有效 token 直接运行、401 触发重新登录、登录失败不继续、finally 清理环境变量等路径；测试输出只检查状态和非敏感错误，不打印凭据。
   - 运行现有 Workflow transport/API 相关测试，确认公开 API 仍要求 bearer token，未改变权限边界。

这样没有 `dsh-client` 的电脑只需要能运行 PowerShell 和访问 Workflow API，就能在首次运行时登录；已有 machine token 的电脑继续兼容现有凭据文件和启动方式。